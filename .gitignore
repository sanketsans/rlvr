.env
outputs/
.cache/
*.egg-info/
__pycache__/
.pytest_cache/
.ruff_cache/
models/
wandb/
skypilot/
# Anchor project-root dirs with a leading slash so they don't also match
# same-named source packages (e.g. src/qwen3_rlvr/data/) — an unanchored
# `data/` matches at ANY depth and was excluding the data package from
# SkyPilot's workdir sync, causing `ModuleNotFoundError: qwen3_rlvr.data`.
/docs/
/experiments/
/data/
